Crie uma constante chamada database, que deverá ser uma lista de objetos(carros)
{
    id: number,
    modelo: string,
    ano: number,
    cor: string
}

Crie 5 rotas que irao manipular a constante database, as rotas serao:
Criar
Buscar Todos
Buscar um pelo id
Atualizar
Remover